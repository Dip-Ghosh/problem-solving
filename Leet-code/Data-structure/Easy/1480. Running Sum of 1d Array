class Solution {
public:
    vector<int> runningSum(vector<int>& nums) {

        vector<int> newArray;
        int sum =0;

        for(int i=0; i< nums.size(); i++)
        {
            sum += nums[i];
            newArray.push_back(sum);
        }
        return newArray;
    }
};