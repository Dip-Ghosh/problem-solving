class Solution {
public:
    vector<int> buildArray(vector<int>& nums) {

        vector<int> newArray;
        int size = nums.size();

        for (int i=0; i<size; i++)
        {
            newArray.push_back(nums[nums[i]]);
        }

        return newArray;
    }
};