#include<iostream>
#include<math.h>
using namespace std;

int main()
{
    int number;
    cout << "Enter the number that you want to find the square root: ";
    cin >> number;
    cout << sqrt(number);
    return 0;
}
